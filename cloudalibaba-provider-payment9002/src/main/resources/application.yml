server:
  port: 9002

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.104 #配置nacos地址

management:
  endpoints:
    web:
      exposure:
        include: '*'
